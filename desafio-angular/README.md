# DesafioAngular (limpio)

Este repositorio ha sido limpiado para comenzar a desarrollar desde cero.

Estructura mínima:

- `src/` — código fuente de la aplicación

Para arrancar el proyecto durante el desarrollo:

```bash
npm install
ng serve
```

Empieza a editar `src/app` y agrega tus componentes, rutas y estilos.
